<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Ativar Autenticação de Dois Fatores</title>
    <link rel="stylesheet" href="/public/style.css">
</head>
<body>
    <form action="/enable-2fa" method="POST" style="text-align: center;">
        <h2>Ativar 2FA</h2>
        <p>Escaneie o QR Code abaixo com seu aplicativo de autenticação (Google Authenticator, Authy, etc.).</p>
        
        <!-- O QR Code será inserido aqui pelo EJS -->
        <img src="<%= qrCodeUrl %>" alt="QR Code para 2FA">

        <p style="margin-top: 20px;">Depois, insira o código de 6 dígitos gerado pelo aplicativo para confirmar.</p>
        
        <input type="text" name="token" placeholder="Código de 6 dígitos" required autocomplete="off" style="text-align: center;">
        
        <button type="submit">Ativar e Verificar</button>

        <% if (typeof erro !== 'undefined' && erro) { %>
            <p style="color: #ffcccc; margin-top: 15px;"><%= erro %></p>
        <% } %>
    </form>
</body>
</html>
